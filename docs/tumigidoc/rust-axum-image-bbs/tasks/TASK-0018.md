# TASK-0018: 主要機能の統合テスト追加

- **タスクタイプ**: TDD
- **推定工数**: 4時間
- **フェーズ**: Phase 3（仕上げ）
- **前提タスク**: TASK-0017
- **後続タスク**: TASK-0019

## 関連文書

- [📋 overview.md](overview.md)
- [📋 requirements.md](../spec/requirements.md)
- [📐 architecture.md](../design/architecture.md)
- [✅ acceptance-criteria.md](../spec/acceptance-criteria.md)
- [🔌 api-endpoints.md](../design/api-endpoints.md)

## タスク概要

TASK-0017 で整えた `tests/common/mod.rs` を使って、受け入れ基準の主要シナリオを `tests/integration_*.rs` に実装する。対象は一覧（TC-001-02 / TC-001-03）、投稿作成（TC-004-01 / TC-004-E01 / TC-004-E04）、編集（TC-009-01 / TC-009-E01）、削除（TC-010-01）。multipart は手書きで組み立てるか `reqwest::multipart` をオフラインで活用し、本番ハンドラを `oneshot` で叩いてアサートする。 🔵

## 依存タスク

- TASK-0017（テスト基盤と smoke テスト）

## 完了条件

- 🔵 4本のテストファイルが作成されている:
  - `tests/integration_list.rs` … TC-001-02, TC-001-03（投稿1件・25件、ページネーション境界）
  - `tests/integration_create.rs` … TC-004-01（画像あり成功）, TC-004-E01（title 空）, TC-004-E04（pdf 拒否）
  - `tests/integration_edit.rs` … TC-009-01（title のみ更新）, TC-009-E01（存在しない id で 404）
  - `tests/integration_delete.rs` … TC-010-01（画像あり投稿の削除＋画像ファイルも消える）
- 🔵 各テストは `common::setup().await` で TestApp を作り、必要なフィクスチャ（投稿レコード・画像ファイル）を INSERT/書き込みしてから検証する
- 🔵 multipart リクエストの組み立ては `tests/common/mod.rs` のヘルパ `multipart_body(fields, files)` として共通化
- 🔵 一覧テストでは HTML レスポンスをパースせず、本文に `<a href="/posts/{id}/edit">` などのキーが含まれることを文字列マッチで検証してよい（学習用途）
- 🔵 削除テストでは「DB に投稿が無い」「`upload_dir/{id}.jpg` が存在しない」両方をアサート
- 🔵 編集 404 テストでは `GET /posts/9999/edit` または `POST /posts/9999/update` で 404 が返る
- 🔵 `cargo test` が全テストグリーンで終了し、`cargo test --tests -- --nocapture` で各シナリオが PASS 表示される
- 🟡 失敗時にどのテストが落ちたかが分かるよう、各テストに十分な assert メッセージを付ける

## 実装詳細

### tests/common/mod.rs への追記 🔵
```rust
pub fn multipart_body(boundary: &str, fields: &[(&str, &str)], file: Option<(&str, &str, &[u8])>) -> Vec<u8> {
    let mut out = Vec::new();
    for (name, value) in fields {
        out.extend_from_slice(format!("--{boundary}\r\n").as_bytes());
        out.extend_from_slice(format!("Content-Disposition: form-data; name=\"{name}\"\r\n\r\n").as_bytes());
        out.extend_from_slice(value.as_bytes());
        out.extend_from_slice(b"\r\n");
    }
    if let Some((name, filename, bytes)) = file {
        out.extend_from_slice(format!("--{boundary}\r\n").as_bytes());
        out.extend_from_slice(format!("Content-Disposition: form-data; name=\"{name}\"; filename=\"{filename}\"\r\nContent-Type: image/jpeg\r\n\r\n").as_bytes());
        out.extend_from_slice(bytes);
        out.extend_from_slice(b"\r\n");
    }
    out.extend_from_slice(format!("--{boundary}--\r\n").as_bytes());
    out
}
```

### tests/integration_create.rs の例 🔵
```rust
mod common;
use tower::ServiceExt;
use axum::body::Body;
use axum::http::{Request, StatusCode};

#[tokio::test]
async fn tc_004_01_create_with_jpeg_succeeds() {
    let app = common::setup().await;
    let boundary = "----test";
    let jpeg = include_bytes!("fixtures/sample.jpg");
    let body = common::multipart_body(boundary,
        &[("title","hi"),("text","body")],
        Some(("image","sample.jpg", jpeg)));
    let req = Request::builder()
        .method("POST").uri("/posts")
        .header("content-type", format!("multipart/form-data; boundary={boundary}"))
        .body(Body::from(body)).unwrap();
    let res = app.router.clone().oneshot(req).await.unwrap();
    assert_eq!(res.status(), StatusCode::SEE_OTHER);
    let cnt: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM post").fetch_one(&app.db).await.unwrap();
    assert_eq!(cnt, 1);
}
```

### tests/fixtures/sample.jpg 🟡
- 最小限の有効 JPEG（数十バイトで十分）。infer/imageformat 判定が通る程度の SOI/APP0/EOI を含めた1枚を `git` に commit

### 一覧テスト（TC-001-03）🔵
- ループで 25件 INSERT（画像なしでよい）→ `GET /` で1ページ目、`GET /?page=2` で2ページ目を確認
- 本文に「ページ 1/2」「ページ 2/2」相当のリンクがあること（templates の文言に合わせる）

### 削除テスト（TC-010-01）🔵
- INSERT + 画像ファイル作成 → `POST /posts/{id}/delete` → 303 + DB 0件 + ファイル不在

## 単体テスト要件（TDD 観点）

- 既存のユニットテストは触らない。本タスクは「統合テストの追加」に専念

## 統合テスト要件

- 🔵 TC-001-02: 1件投稿（画像あり）が一覧に表示される
- 🔵 TC-001-03: 25件投稿時に 2ページに分割される
- 🔵 TC-004-01: title + text + jpg で 303 リダイレクト + DB 1件
- 🔵 TC-004-E01: title 空で 400 + 入力値保持（text 値が HTML 本文に含まれる）
- 🔵 TC-004-E04: pdf を image に渡して 400 + 「許可されていない形式」相当メッセージ
- 🔵 TC-009-01: 既存投稿の title を更新 → 303 + DB の title が更新後の値
- 🔵 TC-009-E01: 存在しない id で 404
- 🔵 TC-010-01: 画像あり投稿を削除 → DB 0件 + 画像ファイル削除済み

## Zenn 記事ドラフト

### 記事タイトル案
- 「Rust + axum で画像掲示板を作る (18) - 主要機能の統合テストを書く」

### 想定見出し（H2）
1. 受け入れ基準 → 統合テスト写像のやり方
2. multipart リクエストを手書きで組み立てる
3. `oneshot` でルーターを叩く流儀
4. DB とファイルシステム両方をアサートする
5. テストが落ちたときの読み解き方

### 伝える主なポイント
- 「ユーザー視点の HTTP I/O」を多めに、内部関数のモックは最小限に
- TC 番号を関数名に入れておくと acceptance-criteria.md と紐付けやすい
- fixtures は最小サイズで commit して再現性を確保
- `oneshot` は `Router` の参照を消費するので `.clone()` するか毎回 setup する

### コード例として載せる断片
- `multipart_body` ヘルパ
- create / delete / list のテスト1件ずつ
- fixtures 配置の Cargo.toml ヒント

## 実装手順（TDD タイプ）

1. `/tumigi:tdd-requirements` で対象 TC を確認
2. `/tumigi:tdd-testcases` でケース粒度を決め、ファイル分割方針を確定
3. `/tumigi:tdd-red` で1ファイルずつテストを書き、最初は失敗を確認
4. `/tumigi:tdd-green` で必要ならハンドラ側の細かい挙動を調整
5. `/tumigi:tdd-refactor` で `multipart_body` などを common に集約
6. `/tumigi:tdd-verify-complete` で `cargo test` 全体グリーン

## 注意事項

- 🔵 ハンドラ側に手を入れる場合は最小限に。テストの都合で本実装を歪めない
- 🟡 sqlite in-memory は max_connections=1 にしておくと並列テストで衝突しにくい
- 🟡 fixtures の画像が大きすぎると repo が太るので、最小 JPEG を1〜2枚に絞る
- 🔵 関連要件: TC-001-*, TC-004-*, TC-009-*, TC-010-*（acceptance-criteria.md 参照）, NFR-303

## 信頼性レベルサマリー

- 🔵 青信号: 13件（約81%）
- 🟡 黄信号: 3件（約19%）
- 🔴 赤信号: 0件

**品質評価**: 高品質
